import React from "react";
import { Box, Typography } from "@mui/material";
import '../../styles/Footer.scss'
const Footer = () => (
  <Box mt={5}>
    <Typography className="header-type">
      <b className="">@Made with React Material UI</b>
    </Typography>
  </Box>
);

export default Footer;
